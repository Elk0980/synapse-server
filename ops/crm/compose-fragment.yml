services:
  crm:
    build:
      context: ./ops/crm
    restart: always
    environment:
      PORT: "8080"
      DATABASE_PATH: /data/crm.sqlite
    expose:
      - "8080"
    volumes:
      - crm_data:/data

volumes:
  crm_data:
